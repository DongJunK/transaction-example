spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/transaction_example?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: saficket
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: none
